# Release v1.0.0 — merge `dev` → `main`

| Field | Value |
|-------|-------|
| **Date** | 2026-06-03 |
| **Tag** | `v1.0.0` |
| **Source** | `dev` fast-forward → `main` @ `72bbc4e` |
| **Plan** | [`docs/RELEASE_PLAN.md`](../../RELEASE_PLAN.md) |

---

## Milestone: minimum functional proven

All Coding Task rows B1–B9 marked DONE in `docs/00-brief-alignment.md`.

---

## Pre-merge verification

| Check | Result |
|-------|--------|
| `python scripts/run_quality_gates.py` | PASS (10 unit + 7 integration + 2 e2e) |
| CI on `dev` | Green ([run 26903747487](https://github.com/peluzzza/PoCAssistantZooplus/actions/runs/26903747487)) |
| User authorization | Merge to `main` per milestone |

---

## Git commands executed

```bash
git checkout main && git pull origin main
git merge dev -m "release: v1.0.0 MVP — async /chat, RAG, guardrails, dual-lane"
git tag -a v1.0.0 -m "v1.0.0 — zooplus Assistant PoC MVP (brief-complete)"
git push origin main
git push origin v1.0.0
```

---

## What ships in v1.0.0

- `POST /chat` with site-scoped RAG and topic guard
- CLI `eda`, `ingest`; quality workflow in CI
- OpenCode agents + MCP routes
- Full trace under `docs/trace/`

---

## Post-release

- Continue feature work on `dev` toward **v1.1.0** (streaming, golden queries, G1 load test).
