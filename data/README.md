# Data

| Path | Rule |
|------|------|
| `raw/product_catalog_dataset.json` | **Immutable** — copy of brief dataset; never edit in place |
| `instructions/` (under `docs/`) | Original brief files (DOCX + JSON source) |

Ingest and EDA read from `raw/` only.
